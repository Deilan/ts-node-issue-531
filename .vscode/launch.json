{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to nodemon",
            "type": "node",
            "request": "attach",
            "port": 5858,
            "protocol": "inspector",
            "cwd": "${workspaceRoot}"
        }
    ]
}